'use client'
import { useState } from 'react'
import { useMvp } from '@/lib/mvpStore'
export default function Contracts(){const {state}=useMvp(); const [sel,setSel]=useState<any>(null); const rows=state.contracts; return <div className='page-scroll'><div className='page-header'><div><div className='page-title'>CONTRACTS</div></div></div><div className='page-body'>{rows.length===0?<div className='empty-state'><div className='empty-title'>No contracts yet</div></div>:<div className='table-wrap'><table className='tbl'><thead><tr><th>ID</th><th>TASK</th><th>NEXT RUN</th><th>VALUE</th><th>STATUS</th></tr></thead><tbody>{rows.map((c:any)=><tr key={c.id} onClick={()=>setSel(c)} style={{cursor:'pointer'}}><td>{c.id}</td><td>{c.taskTitle}</td><td>{new Date(c.nextRunDate).toLocaleDateString()}</td><td>${c.monthlyValue}/mo</td><td><span className='badge b-green'>{c.status}</span></td></tr>)}</tbody></table></div>}</div>{sel&&<div className='modal-overlay open' onClick={()=>setSel(null)}><div className='modal' onClick={e=>e.stopPropagation()}><h3>Contract details</h3><div>Related task: {sel.createdFromTaskId}</div><div>SLA: {sel.sla}%</div><div>Assigned agents: {sel.agentName}</div><div className='modal-actions'><button className='mbtn-cancel'>Pause Contract</button><button className='mbtn-ok'>Run Now</button><button className='mbtn-cancel'>Cancel Contract</button></div></div></div>}</div>}
