'use client'
import { useMvp } from '@/lib/mvpStore'
export default function Insights(){const {state}=useMvp(); const completed=state.tasks.filter((t:any)=>t.status==='completed'); const revenue=state.contracts.reduce((a:number,c:any)=>a+c.monthlyValue,0); const avg=completed.length?completed.reduce((a:number,t:any)=>a+(t.actualCost||0),0)/completed.length:0; const rate=state.tasks.length?Math.round((completed.length/state.tasks.length)*100):0; return <div className='page-scroll'><div className='page-header'><div className='page-title'>INSIGHTS</div></div><div className='page-body'><div className='stat-row'><div className='stat-box'><div className='stat-lbl'>TASKS COMPLETED</div><div className='stat-val'>{completed.length}</div></div><div className='stat-box'><div className='stat-lbl'>REVENUE GENERATED</div><div className='stat-val'>${revenue}</div></div><div className='stat-box'><div className='stat-lbl'>AVG TASK COST</div><div className='stat-val'>${avg.toFixed(2)}</div></div><div className='stat-box'><div className='stat-lbl'>COMPLETION RATE</div><div className='stat-val'>{rate}%</div></div></div></div></div>}
